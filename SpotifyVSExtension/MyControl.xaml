<UserControl x:Class="jla.SpotifyVSExtension.MyControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             Background="{DynamicResource VsBrush.Window}"
             Foreground="{DynamicResource VsBrush.WindowText}"
             mc:Ignorable="d"
             d:DesignHeight="170" d:DesignWidth="290"
             Name="MyToolWindow">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="20"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="10"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>
            
            <TextBlock x:Name="TrackName" Grid.Row="0" Grid.Column="2" FontSize="20"/>
            <TextBlock x:Name="ArtistName" Grid.Row="1" Grid.Column="2" FontSize="20"/>
            <Image x:Name="AlbumCover" Grid.Row="0" Grid.RowSpan="2" Grid.Column="0" Width="100" Height="100"/>
        </Grid>
        
        <StackPanel Orientation="Horizontal" Grid.Row="2">
            <Button Content="&#xE100;" FontFamily="Segoe UI Symbol" FontSize="20" Click="PreviousTrackClick" Width="35" Height="35" Name="PreviousButton" Margin="0 0 10 0"/>
            <Button Content="&#xE102;" FontFamily="Segoe UI Symbol" FontSize="20" Click="PlayPauseClick" Width="35" Height="35" Name="PlayPauseButton" Margin="0 0 10 0"/>
            <Button Content="&#xE101;" FontFamily="Segoe UI Symbol" FontSize="20" Click="NextTrackClick" Width="35" Height="35" Name="NextButton" Margin="0 0 30 0"/>

            <Button Content="&#x1f509;" FontFamily="Segoe UI Symbol" FontSize="20" Click="VolumeDownClick" Width="35" Height="35" Name="VolumeDownButton" Margin="0 0 10 0"/>
            <Button Content="&#x1f50a;" FontFamily="Segoe UI Symbol" FontSize="20" Click="VolumeUpClick" Width="35" Height="35" Name="VolumeUpButton" Margin="0 0 10 0"/>
            <Button Content="&#x1f507;" FontFamily="Segoe UI Symbol" FontSize="20" Click="MuteClick" Width="35" Height="35" Name="MuteButton" Margin="0 0 10 0"/>
        </StackPanel>
    </Grid>
</UserControl>
